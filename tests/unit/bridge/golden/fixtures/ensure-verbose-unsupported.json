{
  "record": [
    "run(acpx --format quiet --cwd /tmp/backend --approve-all --non-interactive-permissions deny --verbose codex sessions ensure --name backend:demo @120000)",
    "run(acpx --format quiet --cwd /tmp/backend --approve-all --non-interactive-permissions deny codex sessions ensure --name backend:demo @120000)"
  ],
  "outcome": {
    "ok": {}
  }
}
