{
  "record": [
    "runPty(acpx --format quiet --cwd /tmp/backend --approve-all --non-interactive-permissions deny --model gpt-fake codex sessions ensure --name backend:demo @120000)",
    "runCommand(acpx --format quiet --cwd /tmp/backend --approve-all --non-interactive-permissions deny --model gpt-fake codex sessions show backend:demo @30000)"
  ],
  "outcome": {}
}
