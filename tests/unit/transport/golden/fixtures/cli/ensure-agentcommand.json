{
  "record": [
    "runCommand(acpx --format quiet --cwd /tmp/backend --approve-all --non-interactive-permissions deny --agent ./node_modules/.bin/codex-acp sessions ensure --name backend:demo @120000)"
  ],
  "outcome": {}
}
