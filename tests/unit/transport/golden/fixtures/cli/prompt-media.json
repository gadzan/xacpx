{
  "record": [
    "spawnPrompt(acpx --format json --json-strict --cwd /tmp/backend --approve-all --non-interactive-permissions deny codex prompt -s backend:demo --file <prompt-file>)"
  ],
  "outcome": {
    "ok": {
      "text": ""
    }
  }
}
