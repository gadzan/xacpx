{
  "record": [
    "logger.debug(command.parsed)",
    "sessions.getCurrentSession(\"wx:user\")",
    "sessions.getLogicalSessionRecord(\"demo\")",
    "transport.cancel(session(demo/backend:demo))",
    "logger.info(transport.cancel)",
    "logger.info(command.completed)"
  ],
  "outcome": {
    "ok": {
      "text": "cancelled"
    }
  }
}
