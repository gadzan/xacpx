{
  "record": [
    "sessions.tryReserveSessionAliasOperation(\"relay:demo\")",
    "sessions.getSession(\"relay:demo\")",
    "orchestration.listSessionBlockingTasks(\"backend:relay:demo\")",
    "sessions.countAliasesSharingTransport(\"backend:relay:demo\", \"relay:demo\")",
    "sessions.findPhysicalSiblings(session(relay:demo/backend:relay:demo), \"relay:demo\")",
    "transport.deleteSession(session(relay:demo/backend:relay:demo))",
    "sessions.removeSession(\"relay:demo\")",
    "orchestration.purgeSessionReferences(\"backend:relay:demo\")",
    "logger.error(session.orchestration_purge_failed)"
  ],
  "outcome": {
    "ok": {
      "wasActive": false,
      "sharedAliasCount": 0,
      "transportTornDown": true
    }
  }
}
