{
  "record": [
    "sessions.getSession(\"relay:demo\")",
    "sessions.countAliasesSharingTransport(\"backend:relay:demo\", \"relay:demo\")",
    "transport.cancel(session(relay:demo/backend:relay:demo))",
    "transport.freeWarmProcess(session(relay:demo/backend:relay:demo))",
    "sessions.setArchived(\"relay:demo\", true)"
  ],
  "outcome": {}
}
